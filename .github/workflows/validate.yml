name: validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  pilot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - name: Validate normalized tables
        run: Rscript tests/validate_pilot.R data/pilot
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install semantic validators
        run: python -m pip install --requirement requirements-dev.txt
      - name: Parse semantic distributions
        run: |
          python -c 'from rdflib import Graph; [Graph().parse(path) for path in ["dist/pilot/prac.jsonld", "dist/pilot/prac.ttl", "dist/pilot/out.jsonld", "dist/pilot/out.ttl"]]'
      - name: Validate SHACL
        run: |
          pyshacl --shacl schemas/shacl/concepts.ttl dist/pilot/prac.ttl
          pyshacl --shacl schemas/shacl/concepts.ttl dist/pilot/out.ttl
