# ERA-AOM 2026.1 release candidate 1

Status: **release candidate; noncanonical; not publicly deployed**.

## Scope

First professional packaging candidate for normalized livestock AOM. Preserves
2,794 governed concept identifiers and generates:

- SKOS Turtle, JSON-LD, and RDF/XML;
- OWL schema Turtle and RDF/XML for Protégé/WebVOWL;
- governed semantic bindings in Turtle and JSON-LD;
- CSV and Parquet node/edge exports;
- release metadata, citation, licence, manifest, provenance fingerprints, and
  SHA-256 checksums.

Proposed persistent namespace: `https://w3id.org/era-aom/`. Namespace is not
registered yet; candidate URIs must not be represented as live or canonical.

## Automated evidence

- 30,016 livestock triples equivalent across Turtle, JSON-LD, and RDF/XML,
  including explicit inverse hierarchy and four browser top concepts;
- 2,794 unique SKOS concepts with language-tagged preferred labels;
- no dangling internal hierarchy/related targets;
- no hierarchy cycles;
- SHACL conformance;
- 331 OWL schema triples equivalent across Turtle and RDF/XML;
- 9,175 semantic-binding triples equivalent across Turtle and JSON-LD;
- 2,794 visualization nodes and 2,857 edges in CSV/Parquet;
- 2,961 governed feed-material facet assertions in CSV/Parquet;
- 2,152 governed definition enrichments with explicit method and provenance;
- deterministic per-file checksums.

## Gates still open

- appoint permanent vocabulary reviewer;
- approve and register HTTPS namespace;
- deploy content negotiation and run live smoke test;
- load candidate into Fuseki/Skosmos test deployment;
- visually inspect OWL schema in Protégé and WebVOWL;
- register approved release in AgroPortal;
- resolve six comparison exclusions recorded by downstream cutover audit;
- obtain explicit canonical-cutover approval.

Dual publication remains required throughout release-candidate evaluation.
